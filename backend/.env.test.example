# Copy to backend/.env.test (gitignored) before running integration tests.
# Database name must contain "_test" (see backend/tests/setup/env.ts).

TEST_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/weekly_report_generator_test?schema=public"

JWT_SECRET=test-jwt-secret-for-integration-tests
JWT_EXPIRES_IN=1h
